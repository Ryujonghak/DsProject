export default class Wishlist {
    constructor(
        wid,
        username,
        moviecd
    ) {
        this.wid = wid;
        this.username = username;
        this.moviecd = moviecd;
    }
}