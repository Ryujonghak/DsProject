export default class Seat {
    // js 생성자 : constructor(매개변수...) {}
    constructor(
        sno,
        seatposition,
        seatyn,
        scno
    ) {
        this.sno = sno;
        this.seatposition = seatposition;
        this.seatyn = seatyn;
        this.scno = scno;
    }
}